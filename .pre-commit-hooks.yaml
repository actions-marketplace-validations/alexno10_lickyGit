- id: lickygit
  name: lickyGit Secret Scanner
  description: Scan for secrets in staged files before committing.
  entry: lickygit scan --staged --severity high --no-banner
  language: python
  types: [text]
  stages: [pre-commit]
